<!-- 学生案例 -->
<view class="studentcases">
<block wx:for="{{caselist}}" wx:for-index="index" wx:for-item="item">
  <view class="caseitem" bindtap="gotodetail" data-id="{{item.id}}">
    <image src="{{imgpath+item.img}}"></image>
    <view>
      <text class="name">{{item.studentName}}</text>
      <text class="text">{{item.title}}</text>
      <text class="school"><text>{{item.name}}</text></text>
    </view>
  </view>
  </block>
</view>
